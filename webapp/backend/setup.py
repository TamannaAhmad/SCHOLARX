from setuptools import setup, find_packages

setup(
    name="scholarx_backend",
    version="0.1",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # Dependencies will be installed from requirements.txt
    ],
)
